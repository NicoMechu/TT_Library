<h2> Authors </h2>

<ul>
  <% @authors.each do |a| %> 

    <li><%= link_to "#{a.first_name} #{a.last_name}", a %></li>
    
  <% end %>
</ul> 

<% if @authors.count == 0 %>
    <div>There are no authors to show</div>
<% end %>
